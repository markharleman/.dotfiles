- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.*: .*
    glob: true
- create:
    - ~/downloads
    - ~/.vim/undo-history

- shell:
  - [git submodule update --init --recursive, Installing submodules]