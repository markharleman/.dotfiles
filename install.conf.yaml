- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/:
    path: /.*
- create:
    - ~/downloads
    - ~/.vim/undo-history

- shell:
  - [git submodule update --init --recursive, Installing submodules]