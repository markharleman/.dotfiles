- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.config:
    path: .config
    create: true
# - create:
#     - ~/downloads
#     - ~/.vim/undo-history

- shell:
  - [git submodule update --init --recursive, Installing submodules]