- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.*

- create:
    - ~/downloads
    - ~/.vim/undo-history

- shell:
  - [git submodule update --init --recursive, Installing submodules]